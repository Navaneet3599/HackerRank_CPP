#include <cstdio>
using namespace std;

/*
Add `int max_of_four(int a, int b, int c, int d)` here.
*/
int max_of_four(int a, int b, int c, int d)
{
    //Writing a single statement to perform 4 variable comparison
    return ((a > b) || (d > b) || (c > b))? (((a > c) || (d > c))? ((a > d) ? a : d) : c) : b;
}

int main() {
    int a, b, c, d;
    scanf("%d %d %d %d", &a, &b, &c, &d);
    int ans = max_of_four(a, b, c, d);
    printf("%d", ans);
    
    return 0;
}